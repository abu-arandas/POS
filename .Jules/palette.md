## 2024-05-19 - [Form Accessibility Pattern]
**Learning:** Found a widespread pattern in EA POS components where `<label>` elements lack `htmlFor` attributes to associate with their `<input>` fields, impacting screen reader accessibility and decreasing click areas.
**Action:** Always verify new and existing form elements have proper `id` and `htmlFor` associations.
